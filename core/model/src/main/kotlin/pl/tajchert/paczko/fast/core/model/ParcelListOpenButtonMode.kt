package pl.tajchert.paczko.fast.core.model

/**
 * Controls which ready-for-pickup rows expose the remote-open action directly
 * on the parcel list.
 */
enum class ParcelListOpenButtonMode {
    FIRST,
    ALL,
    NONE,
}
