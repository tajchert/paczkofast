// Compose compiler stability configuration.
//
// core:model holds the app's domain models — pure, immutable Kotlin data classes
// (all `val`, no mutable collections). Because core:model is a non-Compose module,
// the Compose compiler cannot infer their stability and treats them as UNSTABLE,
// which stops composables that take these types from skipping recomposition.
//
// Listing them here promises the compiler they are stable. Keep core:model
// genuinely immutable (verified: no `var`/Mutable* types) or this promise breaks.
//
// Applied to every Compose module from the root build.gradle.kts subprojects block.
// Docs: https://developer.android.com/develop/ui/compose/performance/stability/fix#stability-configuration-file
pl.tajchert.paczko.fast.core.model.**
